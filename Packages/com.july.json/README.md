# July JSON

`com.july.json` is the single owner of the LitJson binary used by JulyFramework packages.

Packages that use the `LitJson` namespace must declare a dependency on `com.july.json`. This package intentionally adds no July wrapper or runtime abstraction.
